// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum UserRole {
  CLIENT
  TRANSPORTER
  ADMIN
  BACKOFFICE
}

enum TransporterStatus {
  PENDING_DOCUMENTS
  PENDING_REVIEW
  ACTIVE
  SUSPENDED
}

enum VehicleType {
  CAR
  VAN
  TRUCK
  MOTORCYCLE
}

enum LoadType {
  LIGHT
  HEAVY
  BOTH
}

enum VehicleStatus {
  PENDING_REVIEW
  ACTIVE
  SUSPENDED
}

enum ValidationEntityType {
  VEHICLE
  TRANSPORTER_PROFILE
}

enum RejectionCategory {
  POOR_QUALITY_PHOTOS
  EXPIRED_DOCUMENTS
  INFORMATION_MISMATCH
  ADDITIONAL_DOCUMENTATION_REQUIRED
  OTHER
}

enum ZonePreference {
  PREFERRED
  NEUTRAL
  EXCLUDED
}

model User {
  id                 String              @id @default(uuid()) @db.Uuid
  cognitoSub         String              @unique
  email              String              @unique
  phone              String
  firstName          String
  lastName           String
  role               UserRole            @default(CLIENT)

  // Client-specific (optional)
  companyName        String?
  taxId              String?

  // Relations
  transporterProfile TransporterProfile?
  addresses          Address[]
  vehicles           Vehicle[]
  reviewedValidations ValidationLog[]    @relation("ReviewedValidations")
  transporterValidations ValidationLog[] @relation("TransporterValidations")
  zonePreferences    TransporterZonePreference[] @relation("TransporterZonePreferences")

  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt
}

model TransporterProfile {
  id                   String            @id @default(uuid()) @db.Uuid

  // License
  licenseNumber        String
  licenseExpiration    DateTime
  licenseFrontPhotoUrl String
  licenseBackPhotoUrl  String
  idPhotoUrl           String

  // Address
  address              String
  city                 String
  state                String
  postalCode           String
  country              String

  // Insurance
  insurancePolicy      String
  insuranceExpiration  DateTime
  insuranceDocumentUrl String

  // Status
  status               TransporterStatus @default(PENDING_REVIEW)

  // Relation
  userId               String            @unique @db.Uuid
  user                 User              @relation(fields: [userId], references: [id], onDelete: Cascade)

  createdAt            DateTime          @default(now())
  updatedAt            DateTime          @updatedAt
}

model Address {
  id           String   @id @default(uuid()) @db.Uuid
  alias        String
  street       String
  city         String
  state        String
  postalCode   String
  country      String   @default("Guatemala")
  latitude     Float?
  longitude    Float?
  contactName  String?
  contactPhone String?
  isDefault    Boolean  @default(false)

  // Relation
  userId       String   @db.Uuid
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@index([userId])
}

model Vehicle {
  id                  String        @id @default(uuid()) @db.Uuid

  // Basic Info
  brand               String
  model               String
  year                Int
  licensePlate        String        @unique
  vin                 String        @unique @db.VarChar(17)
  color               String
  vehicleType         VehicleType

  // Capacity
  loadType            LoadType
  maxWeightKg         Float
  maxVolumeM3         Float

  // Photos
  frontPhotoUrl       String
  rearPhotoUrl        String
  sidePhotoUrl        String
  interiorPhotoUrl    String

  // Documents
  registrationDocUrl  String
  insuranceDocUrl     String
  insuranceExpiration DateTime

  // Status
  status              VehicleStatus @default(PENDING_REVIEW)

  // Relation
  userId              String        @db.Uuid
  user                User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  createdAt           DateTime      @default(now())
  updatedAt           DateTime      @updatedAt

  @@index([userId])
}

model ValidationLog {
  id                String               @id @default(uuid()) @db.Uuid
  entityType        ValidationEntityType
  entityId          String               @db.Uuid
  action            String               // APPROVED, REJECTED
  rejectionCategory RejectionCategory?
  rejectionDetails  String?
  checklist         Json?

  // Reviewer (BACKOFFICE/ADMIN)
  reviewedBy        String               @db.Uuid
  reviewer          User                 @relation("ReviewedValidations", fields: [reviewedBy], references: [id])

  // Transporter being validated
  transporterId     String               @db.Uuid
  transporter       User                 @relation("TransporterValidations", fields: [transporterId], references: [id])

  emailSent         Boolean              @default(false)
  createdAt         DateTime             @default(now())

  @@index([entityType, entityId])
  @@index([transporterId])
  @@index([reviewedBy])
}

// ==================== LOCATION MODELS ====================

model Country {
  id        String   @id @default(uuid()) @db.Uuid
  name      String   @unique
  code      String   @unique @db.VarChar(3)
  isActive  Boolean  @default(true)

  states    State[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([code])
  @@index([isActive])
}

model State {
  id        String   @id @default(uuid()) @db.Uuid
  name      String
  code      String   @db.VarChar(10)
  isActive  Boolean  @default(true)

  countryId String   @db.Uuid
  country   Country  @relation(fields: [countryId], references: [id], onDelete: Cascade)

  municipalities Municipality[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([countryId, code])
  @@unique([countryId, name])
  @@index([countryId])
  @@index([isActive])
}

model Municipality {
  id        String   @id @default(uuid()) @db.Uuid
  name      String
  code      String   @db.VarChar(10)
  isActive  Boolean  @default(true)

  stateId   String   @db.Uuid
  state     State    @relation(fields: [stateId], references: [id], onDelete: Cascade)

  zones     Zone[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([stateId, code])
  @@unique([stateId, name])
  @@index([stateId])
  @@index([isActive])
}

model Zone {
  id             String   @id @default(uuid()) @db.Uuid
  name           String
  code           String   @db.VarChar(10)
  latitude       Float?
  longitude      Float?
  polygon        Json?
  isActive       Boolean  @default(true)

  municipalityId String   @db.Uuid
  municipality   Municipality @relation(fields: [municipalityId], references: [id], onDelete: Cascade)

  transporterPreferences TransporterZonePreference[]

  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@unique([municipalityId, code])
  @@unique([municipalityId, name])
  @@index([municipalityId])
  @@index([isActive])
}

model TransporterZonePreference {
  id            String         @id @default(uuid()) @db.Uuid
  preference    ZonePreference @default(NEUTRAL)

  transporterId String         @db.Uuid
  transporter   User           @relation("TransporterZonePreferences", fields: [transporterId], references: [id], onDelete: Cascade)

  zoneId        String         @db.Uuid
  zone          Zone           @relation(fields: [zoneId], references: [id], onDelete: Cascade)

  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt

  @@unique([transporterId, zoneId])
  @@index([transporterId])
  @@index([zoneId])
  @@index([preference])
}
